name: Build and Push Demo Site

# Πότε θα τρέχει αυτός ο αυτοματισμός;
on:
  push:
    branches:
      - main
    paths:
      - 'sites/demo-menu/**'
      - 'k8s-cluster/**'
      - '.github/workflows/**'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    # Δίνουμε τα απαραίτητα δικαιώματα για να ανεβάσει το image
    permissions:
      contents: read
      packages: write

    steps:
      - name: Κατέβασμα του κώδικα
        uses: actions/checkout@v4

      - name: Σύνδεση στο GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push for ARM
        uses: docker/build-push-action@v5
        with:
          context: sites/demo-menu/
          push: true
          tags: ghcr.io/dmikropoulos/digitech-hosting/sites/demo-menu:latest
          platforms: linux/arm64
            
      - name: Setup Kubernetes context
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBE_CONFIG }}" > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Deploy to Oracle Cloud (K3s)
        run: |
          kubectl apply -f k8s-cluster/demo-menu.yaml --insecure-skip-tls-verify
          kubectl rollout restart deployment demo-menu --insecure-skip-tls-verify
